<header class="masthead mb-auto">
    <div class="inner">
        <h3 class="masthead-brand"><a href="{{ url("homepage.index") }}">Mon Site Web</a></h3>
      <nav class="nav nav-masthead justify-content-center">
          {% if is_granted('IS_AUTHENTICATED_FULLY') %}

                    <a class="nav-link active" href="{{ url("homepage.index") }}">Accueil</a>
               <a class="nav-link" href="{{ url("app_register") }}">Inscription</a>
                    <a class="nav-link" href="{{ logout_url() }}">Déconnexion</a>

                {% else %}
                    
        <a class="nav-link active" href="{{ url("homepage.index") }}">Accueil</a>
        <a class="nav-link" href="{{ url("app_register") }}">Inscription</a>
        <a class="nav-link" href="{{ url("app_login") }}">Connexion</a>
        
        {% endif %}
      </nav>
    </div>
  </header>


